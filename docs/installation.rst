============
Installation
============

At the command line::

    $ easy_install django_private_chat2

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django_private_chat2
    $ pip install django_private_chat2
